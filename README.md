#What it would look like in python

#name=input("Hi! What is your name?: ")
#print(f"Nice to meet you {name}!\nLet's see if you can guess the number I am thinking of.")
#number=56
#attempts=0
#while number:
    #guess=input("Enter a number between 1 and 100: ")
    #attempts+=1
    #if guess>number:
        #print(f"Attempt {attempts}: The number you have entered is too high. Please try again.")
    #elif guess<number:
        #print(f"Attempt {attempts}: The number you have entered is too low. Please try again.")
    #else:
        #print(f"Attempt {attempts}: Congradulations! You have correctly guessed the right number!")
        #if attempts>7:
            #print(f"You won my game in {attempts} attempts. That is more than the average player.")
        #elif attempts<7:
            #print(f"You won my game in {attempts} attempts. That is less than the average player. Great job!")
        #else:
            #print(f"You won my game in {attempts} attempts. That is about average among players. Good job!")